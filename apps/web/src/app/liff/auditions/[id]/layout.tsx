'use client'

import React from 'react'

/**
 * オーディション詳細ページ用のレイアウト
 * BottomNavを非表示にして、詳細ページ専用の応募ボタンを表示
 */
export default function AuditionDetailLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
