'use client'

// ここにv0からコピーしたコードを貼り付けます
// 例：

import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function V0ExamplePage() {
  return (
    <div className="container mx-auto p-8">
      <h1 className="text-3xl font-bold mb-6">v0で生成したデザイン</h1>
      
      <Card>
        <CardHeader>
          <CardTitle>サンプルカード</CardTitle>
          <CardDescription>
            v0からコピーしたコンポーネントをここに貼り付けます
          </CardDescription>
        </CardHeader>
        <CardContent>
          <Button>テストボタン</Button>
        </CardContent>
      </Card>
      
      {/* v0のコードをここに追加 */}
    </div>
  )
}
